plugins {
	id 'java'
	id 'org.springframework.boot' version '4.1.0'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.module06'
version = '0.0.1-SNAPSHOT'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(17)
	}
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-actuator'

	// Spring Data JPA: 엔티티 매핑과 Repository 기반의 데이터베이스 접근을 지원합니다.
	// implementation 'org.springframework.boot:spring-boot-starter-data-jpa'

	// Flyway: SQL 마이그레이션 파일을 버전별로 실행하여 데이터베이스 스키마 변경 이력을 관리합니다.
	// implementation 'org.springframework.boot:spring-boot-starter-flyway'

	implementation 'org.springframework.boot:spring-boot-starter-security'
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	implementation 'org.springframework.boot:spring-boot-starter-webmvc'

	// Flyway MySQL 지원: Flyway가 MySQL 전용 기능과 마이그레이션을 처리할 수 있게 합니다.
	// implementation 'org.flywaydb:flyway-mysql'

	compileOnly 'org.projectlombok:lombok'
	developmentOnly 'org.springframework.boot:spring-boot-devtools'

	// MySQL JDBC 드라이버: 실행 중인 애플리케이션이 MySQL 데이터베이스에 연결할 수 있게 합니다.
	// runtimeOnly 'com.mysql:mysql-connector-j'

	annotationProcessor 'org.projectlombok:lombok'
	testImplementation 'org.springframework.boot:spring-boot-starter-actuator-test'

	// Spring Data JPA 테스트: JPA Repository와 영속성 계층 테스트에 필요한 도구를 제공합니다.
	// testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa-test'

	// Flyway 테스트: 테스트 환경에서 데이터베이스 마이그레이션 동작을 검증할 수 있게 합니다.
	// testImplementation 'org.springframework.boot:spring-boot-starter-flyway-test'

	testImplementation 'org.springframework.boot:spring-boot-starter-security-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-validation-test'
	testImplementation 'org.springframework.boot:spring-boot-starter-webmvc-test'
	testCompileOnly 'org.projectlombok:lombok'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
	testAnnotationProcessor 'org.projectlombok:lombok'
}

tasks.named('test') {
	useJUnitPlatform()
}
