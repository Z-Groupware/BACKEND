-- =====================================================================
-- V7.8 : handover_item에 액션 생성시각 스냅샷(action_created_at_snap) 추가 (박종준 레인 V7.x)
-- ---------------------------------------------------------------------
-- FE 인수인계 상세의 "인계 액션 타임라인" 간트 막대에서 막대 왼쪽 끝(시작)을 그리는 소스.
-- 막대 오른쪽 끝 = deadline_snap(기존), 왼쪽 끝(시작) = 액션이 생성된 시점.
-- 인계 생성 시점에 C(액션) 모듈에서 받아 스냅샷으로 고정한다. NULL 허용(레거시/미제공 액션).
-- =====================================================================

ALTER TABLE handover_item
    ADD COLUMN action_created_at_snap DATETIME NULL COMMENT '액션 생성시각 스냅샷(타임라인 막대 시작점)';
