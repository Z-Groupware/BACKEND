-- =====================================================================
-- V2.3.17 : team.parent_team_id 제거 — 본부>팀 계층 폐지       [담당: 윤종호]
-- ---------------------------------------------------------------------
-- 배경:
--   V2.3.12가 추가한 본부>팀 2계층(parent_team_id)을 폐지한다(2026-08-07
--   결정). "부서 안의 역할"(개발팀 > 프론트/백엔드)은 team이 아니라
--   role 테이블(V2.3.4)이 담당하므로, team 자신은 부모를 가질 필요가 없다.
--
-- V2.3.16이 먼저 적용돼 있어야 한다 — name_scope_parent_id 생성 컬럼이
-- 이 컬럼을 참조해서, 이 컬럼을 먼저 지우면 그 생성 컬럼이 깨진다.
-- =====================================================================

ALTER TABLE `team`
    DROP FOREIGN KEY `FK_TEAM_PARENT_TEAM`,
    DROP COLUMN `parent_team_id`;
