-- 연차는 이번 범위에서 다루지 않는다.
--
-- 컬럼을 남겨두면 "언젠가 쓸 것"으로 읽혀 마이페이지 API 를 짤 때 채우려는 시도가 나온다.
-- 부여 일수만 있고 사용 일수·발생 규칙이 없어서 채워도 의미 있는 값이 안 나온다.
-- 필요해지면 그때 사용 이력까지 갖춘 설계로 다시 넣는다.
--
-- ⚠️ 되돌릴 수 없다. 값이 들어 있었다면 함께 사라진다 — 현재 이 컬럼을 쓰는 코드는 없다.
ALTER TABLE `member`
    DROP COLUMN `annual_leave_granted`;
