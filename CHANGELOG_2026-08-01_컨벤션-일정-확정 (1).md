# 📢 잇다(Z) 컨벤션·일정 변경 공유 (2026-08-01)

> **CLAUDE.md ver 1.4 → 1.5** / **PLAN.md ver 1.0 → 1.2**
> 확정: 박종준(PO). 파일 위치: `CLAUDE.md`(루트) · `docs/PLAN.md`
> 커밋·PR은 별도 진행 (컨벤션 변경은 사람 PR로 — CLAUDE.md 12절)

---

## 0. 결정 요약

| # | 항목 | 결정 | 상태 |
| --- | --- | --- | --- |
| 1 | 캡처 파이프라인 | **X안** — 요약 입력=음성 비동기 처리 | ✅ 확정 |
| 2 | 메모 기능 | **삭제** (`Memo` 모델·참석자 제출 제거) | ✅ 확정 |
| 3 | 실시간 자막 | 담당자 화면 **로컬 표시만** (socket.io 중계 제거) | ✅ 확정 |
| 4 | `TranscriptChunk` | **삭제** (담당자 자막은 로컬 휘발) | ✅ 확정 |
| 5 | billing·notice | **스코프 아웃** (3주 제외) | ✅ 확정 |
| 6 | 파일(F 접두어) | **storage 흡수** | ✅ 확정 |
| 7 | FE 배포 | **EC2** | ✅ 확정 |
| 8 | 프로젝트 조회 권한 | 생성만 OWNER / 조회=소속 부서 MEMBER 이상 | ⚠️ **김민섭 확인 대기** |

---

## 1. 캡처 파이프라인 — 옵션 A 폐기, X안 확정

**이전(옵션 A):** STT 텍스트=요약 입력 / 음성=아카이브(요약 아님) / 메모=제출 즉시 저장
**변경(X안):** **음성파일=요약 입력(비동기 처리) 겸 다시듣기 아카이브** / 담당자 자막=순수 프론트, 저장·중계 안 함 / **메모 없음**

### 파이프라인 (신)
| 단계 | 처리 |
| --- | --- |
| 1 | 담당자 녹음 + 브라우저 STT 자막을 **담당자 화면에 로컬 표시** (웹소켓 없음·순수 FE·저장 안 함) |
| 2 | **음성을 비동기로 잘라 BE 업로드**(청크 단위) → BE 수신·저장 |
| 3 | 업로드 완료 → 회의 종료 버튼 |
| 4 | 회의 종료 → **음성 처리 결과로 AI 요약** → 담당자 자동배정 → 액션 자동생성 |
| 5 | 회의 상세 = 요약 + 다시듣기(음성 아카이브) |

### 딸려온 삭제/정리
- `Memo`·`TranscriptChunk` 모델 **삭제** → capture 모델은 `Recording` 하나
- **socket.io 프로젝트에서 미사용** (자막 중계·메모 제출 둘 다 제거)
- 회의 `Attendee.isSubmitted`(제출 여부) 필드 **삭제** (메모 연동이라 고아됨). 참석 확인 필요 시 별도 필드로
- 미지원 브라우저 = 담당자 자막만 안 보임, **요약엔 영향 없음**(음성 기반)
- 음성 요약 방식(서버 STT 전사 vs 멀티모달 직접)은 **BE 스파이크에서 결정** — 계약 무관

### 문서 반영 위치 (CLAUDE.md)
3절 캡처 책임 · 2.4 필드표(청크/메모/제출여부 삭제) · 2.3 공통필드 · 4절 회의모델·캡처모델·파이프라인·규칙 · 13절 협의 4건 종료

---

## 2. 미확정 4건 처리

| 항목 | 결정 | 반영 |
| --- | --- | --- |
| **billing·notice** | 스코프 아웃 (3주 제외) | ErrorCode `PY`·`NO` 및 ERD에서 제외. 발표 후 재검토 |
| **파일(F 접두어)** | `storage` 흡수 | 별도 도메인 없음, `storage` 안에서 `F` 접두어 사용 |
| **FE 배포** | EC2 | BE와 동일 AWS. F 도메인 배포 선행조건 해소 |
| **프로젝트 조회 권한** | ⚠️ 아래 3번 참고 | 잠정 반영 |

---

## 3. ⚠️ 프로젝트 조회 권한 — 김민섭 확인 필요

**문제:** 6절 매트릭스가 "프로젝트 생성·조회 둘 다 OWNER"로 묶여 있었는데, 4절 정책(생성만 오너)과 **모순**. 또 MEMBER가 자기 액션 마감(프로젝트 마감 참조)을 못 보게 됨.

**제안(잠정 반영):**
- 생성 = **OWNER** 유지
- 조회 = **본인 소속(지정 부서) 프로젝트는 MEMBER 이상 / 전사 목록은 OWNER**

→ **project 도메인 담당(김민섭) 사인오프 후 최종 확정.** 문서엔 6절·4절에 `⚠️ 확인 후 확정`으로 표시, 13절에 `[~]`(진행중)으로 남김. 윤종호(권한/역할)에게도 공유.

---

## 4. 일정(PLAN.md) 변경

- **BE 캡처(A)**: W0 `socket.io PoC` 제거, W1 `청크·메모 스키마·자막 중계` 제거 → 음성 비동기 기준. 난이도 **★★★★★ → ★★★★☆**
- **FE 캡처(A/안현)**: M0 `자막·메모` → **담당자 로컬 자막(메모 없음)**, M1 STT PoC를 담당자 자막용으로 명확화. **M2(업로드+요약 SSE)는 그대로**
- **FE 배포(F)**: `AWS 서비스 확정` → **EC2 확정**
- 정합성 점검: billing·notice(4번)·협의 4건(6번) **해소 표시**

> 🔴 **안현 M0(피그마)가 오늘~8/2** — 메모 화면 빼야 하니 이 공유 먼저 확인 요망.

---

## 5. 파일 위치
- 컨벤션: `MODULE6/CLAUDE.md` (ver 1.5)
- 일정: `MODULE6/docs/PLAN.md` (ver 1.2)
- 이 공유문: `MODULE6/docs/CHANGELOG_2026-08-01_컨벤션-일정-확정.md`
